/**
 * Content width shared by the chess page and its loading skeleton, so the
 * placeholder occupies exactly the space the real content will.
 */
export const CHESS_SHELL = "mx-auto w-full max-w-6xl px-4 sm:px-6";
